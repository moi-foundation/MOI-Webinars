And here is the grant. One interaction â signed by the OWNER, not the agent. Storage of Ticker. Allow mutate. Origin must be the agent. Walk the lines. Then the punchline: the agent is not asked. It cannot decline. It finds out when it tries.