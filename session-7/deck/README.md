# Session 7 deck

`MOI_Builders_S7-new.pptx` — 7 slides. **The speaker notes are the full talk script**; present from
presenter view and read them. TRANSCRIPT.md in the parent folder only keeps what doesn't fit in
notes (the precision list and the Q&A crib).

The deck is edited directly in PowerPoint — there is no generator. (An earlier `build-pptx.cjs` was
removed; regenerating would overwrite hand-made design and pasted notes.)

| | |
| --- | --- |
| flow | title → the two agents → live demo cue → seven files → self-imposed guardrails → sessions 8–9 tease → close |
| palette/type | taken from `MOI_Builders_S5_2.pptx` (kept here for reference) |
| demo cues | slide 3 → browser (`npm run ui`) · slide 4 → editor · slide 5 → terminal env-var beat |
